# Binaries and build output
*.exe
*.out
/bin/

# Test & coverage artifacts
coverage.out
coverage.html
*.test
*.prof

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local-only working docs (build handoff + assistant context); never published
/CLAUDE.md
/PROGRESS.md
/.claude/

# Local Go workspace (dev only; submodules ship depending on tagged core)
/go.work
/go.work.sum
